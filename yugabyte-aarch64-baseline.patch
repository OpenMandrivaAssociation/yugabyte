--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -75,9 +75,8 @@
 endif()
 
 if(CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
-  # Optimize for Graviton on Linux/aarch64 (not mac/arm64)
-  ADD_CXX_FLAGS("-march=armv8.2-a+fp16+rcpc+dotprod+crypto")
-  ADD_CXX_FLAGS("-mtune=neoverse-n1")
+  # Do not force Graviton/Neoverse instructions. The package is built and
+  # run on the distribution baseline, which does not have dotprod.
   ADD_CXX_FLAGS("-mno-outline-atomics")
 endif()
 
