diff -urN orig/yugabyte-db-2026.1.1.0/CMakeLists.txt new/yugabyte-db-2026.1.1.0/CMakeLists.txt
--- a/CMakeLists.txt	2026-08-06 03:52:33.000000000 +0200
+++ b/CMakeLists.txt	2026-08-17 13:46:14.520552314 +0200
@@ -60,7 +60,8 @@
 # We don't use modules, so no need to scan.
 set(CMAKE_CXX_SCAN_FOR_MODULES OFF)
 
-ADD_CXX_FLAGS("-Werror")
+# Distro compilers are newer than Yugabyte's pinned Clang; new warnings must not fail the build.
+ADD_CXX_FLAGS("-Wno-error")
 ADD_CXX_FLAGS("-fno-strict-aliasing -Wall")
 if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(aarch64|arm64)$")
   # Certain platforms such as ARM do not use signed chars by default
@@ -564,9 +565,9 @@
   include_directories(SYSTEM "${LINUXBREW_DIR}/include")
 endif()
 
-if (NOT IS_CLANG)
-  ADD_LINKER_FLAGS("-latomic")
-endif()
+# libstdc++ 16-byte atomics (__atomic_*) need libatomic; clang used to
+# pull this in via the libc++ runtime.
+ADD_LINKER_FLAGS("-latomic")
 
 # Flag to enable clang undefined behavior sanitizer
 # We explicitly don't enable all of the sanitizer flags:
diff -urN orig/yugabyte-db-2026.1.1.0/build-support/common-build-env.sh new/yugabyte-db-2026.1.1.0/build-support/common-build-env.sh
--- a/build-support/common-build-env.sh	2026-08-06 03:52:33.000000000 +0200
+++ b/build-support/common-build-env.sh	2026-08-17 13:47:51.992525650 +0200
@@ -106,6 +106,17 @@
 }
 
 initialize_yugabyte_bash_common() {
+  # Distro packaging: use a pre-extracted yugabyte-bash-common tree and never git clone.
+  if [[ ${YB_SKIP_BASH_COMMON_CLONE:-0} == 1 ]]; then
+    YB_BASH_COMMON_DIR=${YB_BASH_COMMON_DIR:-$YB_SRC_ROOT/build/yugabyte-bash-common}
+    if [[ ! -f $YB_BASH_COMMON_DIR/src/yugabyte-bash-common.sh ]]; then
+      echo >&2 "YB_SKIP_BASH_COMMON_CLONE=1 but $YB_BASH_COMMON_DIR is missing" \
+               "yugabyte-bash-common.sh"
+      exit 1
+    fi
+    return
+  fi
+
   local target_sha1
   target_sha1=$(<"$YB_SRC_ROOT/build-support/yugabyte-bash-common-sha1.txt")
   if [[ ! $target_sha1 =~ ^[0-9a-f]{40}$ ]]; then
@@ -171,7 +182,8 @@
 readonly YB_JENKINS_NFS_HOME_DIR=/Volumes/n/jenkins
 
 # This is the parent directory for all kinds of thirdparty and toolchain tarballs.
-readonly OPT_YB_BUILD_DIR="/opt/yb-build"
+# Distro packaging: keep all of this inside the build root (ABF has no /opt/yb-build).
+readonly OPT_YB_BUILD_DIR="${YB_OPT_BUILD_DIR:-/opt/yb-build}"
 
 readonly LOCAL_THIRDPARTY_DIR_PARENT="$OPT_YB_BUILD_DIR/thirdparty"
 
@@ -1312,6 +1324,12 @@
 }
 
 download_toolchain() {
+  # Distro packaging: use the system compiler, do not download Yugabyte's OS-specific LLVM/GCC.
+  if [[ ${YB_USE_SYSTEM_COMPILER:-0} == 1 ]]; then
+    log "YB_USE_SYSTEM_COMPILER=1, not downloading a Yugabyte LLVM/GCC toolchain"
+    return
+  fi
+
   expect_vars_to_be_set YB_COMPILER_TYPE YB_THIRDPARTY_DIR
   local toolchain_urls=()
   local linuxbrew_url=""
@@ -2109,6 +2127,11 @@
 }
 
 find_or_download_ysql_snapshots() {
+  if [[ ${YB_SKIP_YSQL_SNAPSHOT_DOWNLOAD:-0} == 1 ]]; then
+    log "Skipping prebuilt YSQL snapshot download (YB_SKIP_YSQL_SNAPSHOT_DOWNLOAD=1)"
+    return
+  fi
+
   local repo_url="https://github.com/yugabyte/yugabyte-db-ysql-catalog-snapshots"
   local prefix="initial_sys_catalog_snapshot"
 
@@ -2389,6 +2412,11 @@
   if [[ -n ${YB_VERBOSE:-} ]]; then
     log "Checking syntax of Python scripts"
   fi
+  # Distro tarballs have no .git; git ls-files then feeds nothing to xargs.
+  if [[ ${YB_SKIP_PYTHON_SYNTAX_CHECK:-0} == 1 ]] || \
+     ! git -C "$YB_SRC_ROOT" rev-parse --is-inside-work-tree >/dev/null 2>&1; then
+    return
+  fi
   pushd "$YB_SRC_ROOT"
   local IFS=$'\n'
   # Get all .py files in git, ignoring files with skip-worktree bit set (e.g.
diff -urN orig/yugabyte-db-2026.1.1.0/build-support/fips_install.sh new/yugabyte-db-2026.1.1.0/build-support/fips_install.sh
--- a/build-support/fips_install.sh	2026-08-06 03:52:33.000000000 +0200
+++ b/build-support/fips_install.sh	2026-08-17 13:47:51.992797573 +0200
@@ -17,4 +17,12 @@
 export YB_BIN_DIR="$YB_THIRDPARTY_DIR/installed/common/bin"
 export YB_OSSL_PROVIDER_DIR="$YB_THIRDPARTY_DIR/installed/common/lib/ossl-modules"
 
+# Distro packaging uses system OpenSSL and skips the FIPS provider.
+if [[ ! -x $YB_BIN_DIR/openssl ]] || \
+   [[ ! -e $YB_OSSL_PROVIDER_DIR/fips.so && ! -e $YB_OSSL_PROVIDER_DIR/fips.dylib ]]; then
+  mkdir -p "${1:-.}"
+  : > "${1:-.}/openssl-fips.cnf"
+  exit 0
+fi
+
 "$YB_SRC_ROOT/scripts/installation/bin/fips_install.sh" "$@"
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindAbseil.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindAbseil.cmake
--- a/cmake_modules/FindAbseil.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindAbseil.cmake	2026-08-17 13:46:14.521623357 +0200
@@ -19,6 +19,12 @@
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
 
+# Distro Abseil is split (libabsl_*.so). seed-system-libs.sh writes a
+# GNU ld INPUT() script as libabsl.so; there is no combined .a.
+if(NOT ABSEIL_STATIC_LIB AND ABSEIL_SHARED_LIB)
+  set(ABSEIL_STATIC_LIB ${ABSEIL_SHARED_LIB})
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(ABSEIL REQUIRED_VARS
   ABSEIL_SHARED_LIB ABSEIL_STATIC_LIB)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindGPerf.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindGPerf.cmake
--- a/cmake_modules/FindGPerf.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindGPerf.cmake	2026-08-17 13:46:14.521562296 +0200
@@ -23,6 +23,12 @@
 # or implied.  See the License for the specific language governing permissions and limitations
 # under the License.
 #
+# A previous Google-tcmalloc configure caches TCMALLOC_* as
+# libgoogletcmalloc; that archive needs the bundled Abseil LTS.
+if(TCMALLOC_SHARED_LIB MATCHES "googletcmalloc" OR TCMALLOC_STATIC_LIB MATCHES "googletcmalloc")
+  unset(TCMALLOC_SHARED_LIB CACHE)
+  unset(TCMALLOC_STATIC_LIB CACHE)
+endif()
 FIND_LIBRARY(TCMALLOC_SHARED_LIB tcmalloc
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindHiredis.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindHiredis.cmake
--- a/cmake_modules/FindHiredis.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindHiredis.cmake	2026-08-17 13:46:14.521491125 +0200
@@ -20,6 +20,13 @@
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
 
+# Distro builds may only ship the shared library.
+if(NOT HIREDIS_STATIC_LIB)
+  find_library(HIREDIS_STATIC_LIB hiredis
+    NO_CMAKE_SYSTEM_PATH
+    NO_SYSTEM_ENVIRONMENT_PATH)
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(HIREDIS REQUIRED_VARS
   HIREDIS_STATIC_LIB HIREDIS_INCLUDE_DIR)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindLibBacktrace.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindLibBacktrace.cmake
--- a/cmake_modules/FindLibBacktrace.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindLibBacktrace.cmake	2026-08-17 13:46:14.521417824 +0200
@@ -31,6 +31,11 @@
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
 
+# Distro builds may only ship the shared library.
+if(NOT LIBBACKTRACE_STATIC_LIB AND LIBBACKTRACE_SHARED_LIB)
+  set(LIBBACKTRACE_STATIC_LIB ${LIBBACKTRACE_SHARED_LIB})
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(LIBBACKTRACE REQUIRED_VARS
   LIBBACKTRACE_SHARED_LIB LIBBACKTRACE_STATIC_LIB LIBBACKTRACE_INCLUDE_DIR)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindLibUuid.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindLibUuid.cmake
--- a/cmake_modules/FindLibUuid.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindLibUuid.cmake	2026-08-17 13:46:14.521306393 +0200
@@ -46,6 +46,11 @@
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
 
+# Distro builds may only ship the shared library.
+if(NOT LIBUUID_STATIC_LIB AND LIBUUID_SHARED_LIB)
+  set(LIBUUID_STATIC_LIB ${LIBUUID_SHARED_LIB})
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(LIBUUID REQUIRED_VARS
   LIBUUID_SHARED_LIB LIBUUID_STATIC_LIB LIBUUID_INCLUDE_DIR)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindLibUv.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindLibUv.cmake
--- a/cmake_modules/FindLibUv.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindLibUv.cmake	2026-08-17 13:46:14.521168811 +0200
@@ -31,6 +31,11 @@
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
 
+# Distro builds may only ship the shared library.
+if(NOT LIBUV_STATIC_LIB AND LIBUV_SHARED_LIB)
+  set(LIBUV_STATIC_LIB ${LIBUV_SHARED_LIB})
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(LIBUV REQUIRED_VARS
   LIBUV_SHARED_LIB LIBUV_STATIC_LIB LIBUV_INCLUDE_DIR)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindLz4.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindLz4.cmake
--- a/cmake_modules/FindLz4.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindLz4.cmake	2026-08-17 13:46:14.520896428 +0200
@@ -27,6 +27,12 @@
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
 
+if(NOT LZ4_STATIC_LIB)
+  find_library(LZ4_STATIC_LIB lz4
+    NO_CMAKE_SYSTEM_PATH
+    NO_SYSTEM_ENVIRONMENT_PATH)
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(LZ4 REQUIRED_VARS
   LZ4_STATIC_LIB LZ4_INCLUDE_DIR)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindOtel.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindOtel.cmake
--- a/cmake_modules/FindOtel.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindOtel.cmake	2026-08-17 13:48:12.359461575 +0200
@@ -41,6 +41,10 @@
   NAMES opentelemetry_proto
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
+find_library(OTEL_COMMON_SHARED_LIB
+  NAMES opentelemetry_common
+  NO_CMAKE_SYSTEM_PATH
+  NO_SYSTEM_ENVIRONMENT_PATH)
 
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(OTEL REQUIRED_VARS
@@ -48,5 +52,6 @@
   OTEL_EXPORTER_OTLP_HTTP_SHARED_LIB
   OTEL_RESOURCES_SHARED_LIB
   OTEL_PROTO_SHARED_LIB
+  OTEL_COMMON_SHARED_LIB
   OTEL_INCLUDE_DIR
 )
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindPcre.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindPcre.cmake
--- a/cmake_modules/FindPcre.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindPcre.cmake	2026-08-17 13:46:14.520969099 +0200
@@ -22,6 +22,13 @@
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
 
+# Distro builds may only ship the shared library.
+if(NOT PCRE_STATIC_LIB)
+  find_library(PCRE_STATIC_LIB libpcre2-8.so
+    NO_CMAKE_SYSTEM_PATH
+    NO_SYSTEM_ENVIRONMENT_PATH)
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(PCRE REQUIRED_VARS
   PCRE_STATIC_LIB PCRE_INCLUDE_DIR)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindProtobuf.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindProtobuf.cmake
--- a/cmake_modules/FindProtobuf.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindProtobuf.cmake	2026-08-17 13:46:14.521772288 +0200
@@ -219,6 +219,14 @@
              NO_CMAKE_SYSTEM_PATH
              NO_SYSTEM_ENVIRONMENT_PATH)
 
+# Distro protobuf is shared-only. Fall back so we can use system 32.x.
+if(NOT PROTOBUF_STATIC_LIBRARY AND PROTOBUF_SHARED_LIBRARY)
+  set(PROTOBUF_STATIC_LIBRARY ${PROTOBUF_SHARED_LIBRARY})
+endif()
+if(NOT PROTOBUF_PROTOC_STATIC_LIBRARY AND PROTOBUF_PROTOC_SHARED_LIBRARY)
+  set(PROTOBUF_PROTOC_STATIC_LIBRARY ${PROTOBUF_PROTOC_SHARED_LIBRARY})
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(PROTOBUF REQUIRED_VARS
   PROTOBUF_SHARED_LIBRARY PROTOBUF_STATIC_LIBRARY
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindSnappy.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindSnappy.cmake
--- a/cmake_modules/FindSnappy.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindSnappy.cmake	2026-08-17 13:46:14.521104170 +0200
@@ -31,6 +31,11 @@
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
 
+# Distro builds may only ship the shared library.
+if(NOT SNAPPY_STATIC_LIB AND SNAPPY_SHARED_LIB)
+  set(SNAPPY_STATIC_LIB ${SNAPPY_SHARED_LIB})
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(SNAPPY REQUIRED_VARS
   SNAPPY_SHARED_LIB SNAPPY_STATIC_LIB SNAPPY_INCLUDE_DIR)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/FindZlib.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/FindZlib.cmake
--- a/cmake_modules/FindZlib.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/FindZlib.cmake	2026-08-17 13:46:14.521038270 +0200
@@ -31,6 +31,10 @@
   NO_CMAKE_SYSTEM_PATH
   NO_SYSTEM_ENVIRONMENT_PATH)
 
+if(NOT ZLIB_STATIC_LIB AND ZLIB_SHARED_LIB)
+  set(ZLIB_STATIC_LIB ${ZLIB_SHARED_LIB})
+endif()
+
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(ZLIB REQUIRED_VARS
   ZLIB_SHARED_LIB ZLIB_STATIC_LIB ZLIB_INCLUDE_DIR)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/YugabyteFindThirdParty.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/YugabyteFindThirdParty.cmake
--- a/cmake_modules/YugabyteFindThirdParty.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/YugabyteFindThirdParty.cmake	2026-08-17 13:48:12.400267786 +0200
@@ -156,7 +156,8 @@
   include_directories(SYSTEM ${PROTOBUF_INCLUDE_DIR})
   ADD_THIRDPARTY_LIB(protobuf
     STATIC_LIB "${PROTOBUF_STATIC_LIBRARY}"
-    SHARED_LIB "${PROTOBUF_SHARED_LIBRARY}")
+    SHARED_LIB "${PROTOBUF_SHARED_LIBRARY}"
+    DEPS abseil)
   ADD_THIRDPARTY_LIB(protoc
     STATIC_LIB "${PROTOBUF_PROTOC_STATIC_LIBRARY}"
     SHARED_LIB "${PROTOBUF_PROTOC_SHARED_LIBRARY}"
@@ -270,7 +271,10 @@
   # ------------------------------------------------------------------------------------------------
 
   ## curl
-  find_package(CURL REQUIRED)
+  # Do not load /usr/lib64/cmake/CURL — it add_executable()s the curl tool
+  # and our wrapped add_executable() rejects imported targets.
+  set(CURL_NO_CURL_CMAKE ON)
+  find_package(CURL REQUIRED MODULE)
   include_directories(SYSTEM ${CURL_INCLUDE_DIRS})
   ADD_THIRDPARTY_LIB(curl STATIC_LIB "${CURL_LIBRARIES}")
 
@@ -434,6 +438,8 @@
    SHARED_LIB "${OTEL_RESOURCES_SHARED_LIB}")
   ADD_THIRDPARTY_LIB(opentelemetry_proto
    SHARED_LIB "${OTEL_PROTO_SHARED_LIB}")
+  ADD_THIRDPARTY_LIB(opentelemetry_common
+   SHARED_LIB "${OTEL_COMMON_SHARED_LIB}")
 
   ## libxml2
   find_package(LibXml2 MODULE REQUIRED)
diff -urN orig/yugabyte-db-2026.1.1.0/cmake_modules/YugabyteFunctions.cmake new/yugabyte-db-2026.1.1.0/cmake_modules/YugabyteFunctions.cmake
--- a/cmake_modules/YugabyteFunctions.cmake	2026-08-06 03:52:33.000000000 +0200
+++ b/cmake_modules/YugabyteFunctions.cmake	2026-08-17 13:47:51.991851142 +0200
@@ -449,8 +449,13 @@
       target_link_libraries("${name}" profiler)
     endif()
     if(IS_CLANG)
-      # Static tcmalloc library depends on libc++ when building with Clang.
-      target_link_libraries("${name}" c++)
+      if(DEFINED ENV{YB_USE_SYSTEM_COMPILER} AND "$ENV{YB_USE_SYSTEM_COMPILER}" STREQUAL "1")
+        # System clang + libstdc++. 16-byte atomics need libatomic.
+        target_link_libraries("${name}" atomic)
+      else()
+        # Static tcmalloc library depends on libc++ when building with Clang.
+        target_link_libraries("${name}" c++)
+      endif()
     endif()
     # Link with libm because tcmalloc requires the log2 function.
     target_link_libraries("${name}" m)
@@ -465,79 +470,90 @@
 endfunction()
 
 macro(YB_SETUP_CLANG)
-  ADD_CXX_FLAGS("-stdlib=libc++")
+  # Distro packaging: third-party C++ and system libs (boost, openssl, …)
+  # are libstdc++. Forcing libc++ flags/headers here makes the first link fail.
+  set(_yb_use_system_compiler OFF)
+  if(DEFINED ENV{YB_USE_SYSTEM_COMPILER} AND "$ENV{YB_USE_SYSTEM_COMPILER}" STREQUAL "1")
+    set(_yb_use_system_compiler ON)
+  endif()
+
+  if(NOT _yb_use_system_compiler)
+    ADD_CXX_FLAGS("-stdlib=libc++")
+  endif()
 
   # Disables using the precompiled template specializations for std::string, shared_ptr, etc
   # so that the annotations in the header actually take effect.
   ADD_CXX_FLAGS("-D_GLIBCXX_EXTERN_TEMPLATE=0")
 
   if(NOT IS_APPLE_CLANG)
-    set(LIBCXX_DIR "${YB_THIRDPARTY_INSTALLED_DIR}/${THIRDPARTY_INSTRUMENTATION_TYPE}/libcxx")
-    if(NOT EXISTS "${LIBCXX_DIR}")
-      message(FATAL_ERROR "libc++ directory does not exist: '${LIBCXX_DIR}'")
-    endif()
-    set(LIBCXX_INCLUDE_DIR "${LIBCXX_DIR}/include/c++/v1")
-    if(NOT EXISTS "${LIBCXX_INCLUDE_DIR}")
-      message(FATAL_ERROR "libc++ include directory does not exist: '${LIBCXX_INCLUDE_DIR}'")
-    endif()
-    if(NOT EXISTS "${LIBCXX_DIR}/lib")
-      message(FATAL_ERROR "libc++ library directory does not exist: '${LIBCXX_DIR}/lib'")
-    endif()
-    ADD_GLOBAL_RPATH_ENTRY_AND_LIB_DIR("${LIBCXX_DIR}/lib")
+    if(NOT _yb_use_system_compiler)
+      set(LIBCXX_DIR "${YB_THIRDPARTY_INSTALLED_DIR}/${THIRDPARTY_INSTRUMENTATION_TYPE}/libcxx")
+      if(NOT EXISTS "${LIBCXX_DIR}")
+        message(FATAL_ERROR "libc++ directory does not exist: '${LIBCXX_DIR}'")
+      endif()
+      set(LIBCXX_INCLUDE_DIR "${LIBCXX_DIR}/include/c++/v1")
+      if(NOT EXISTS "${LIBCXX_INCLUDE_DIR}")
+        message(FATAL_ERROR "libc++ include directory does not exist: '${LIBCXX_INCLUDE_DIR}'")
+      endif()
+      if(NOT EXISTS "${LIBCXX_DIR}/lib")
+        message(FATAL_ERROR "libc++ library directory does not exist: '${LIBCXX_DIR}/lib'")
+      endif()
+      ADD_GLOBAL_RPATH_ENTRY_AND_LIB_DIR("${LIBCXX_DIR}/lib")
 
-    # This needs to appear before adding third-party dependencies that have their headers in the
-    # Linuxbrew include directory, because otherwise we'll pick up the standard library headers from
-    # the Linuxbrew include directory too.
-    include_directories(SYSTEM "${LIBCXX_INCLUDE_DIR}")
-
-    execute_process(COMMAND "${CMAKE_CXX_COMPILER}" -print-search-dirs
-                    OUTPUT_VARIABLE CLANG_PRINT_SEARCH_DIRS_OUTPUT)
-
-    if ("${CLANG_PRINT_SEARCH_DIRS_OUTPUT}" MATCHES ".*libraries: =([^:\r\n]+)(:.*|[\r\n]*$)" )
-      # We get a directory like this:
-      # .../yb-llvm-v12.0.1-yb-1-1639783720-bdb147e6-almalinux8-x86_64/lib/clang/12.0.1
-      set(CLANG_LIB_DIR "${CMAKE_MATCH_1}")
-      if(APPLE)
-        set(CLANG_RUNTIME_LIB_DIR "${CLANG_LIB_DIR}/lib/darwin")
-        if(NOT EXISTS "${CLANG_RUNTIME_LIB_DIR}")
-          message(FATAL_ERROR
-                  "Failed to determine Clang runtime library directory inside of "
-                  "${CLANG_RUNTIME_LIB_DIR}")
-        endif()
-      else()
-        set(CLANG_RUNTIME_LIB_DIR "${CLANG_LIB_DIR}/lib/linux")
-        if(NOT EXISTS "${CLANG_RUNTIME_LIB_DIR}")
-          set(CLANG_RUNTIME_LIB_DIR
-              "${CLANG_LIB_DIR}/lib/${CMAKE_SYSTEM_PROCESSOR}-unknown-linux-gnu")
+      # This needs to appear before adding third-party dependencies that have their headers in the
+      # Linuxbrew include directory, because otherwise we'll pick up the standard library headers from
+      # the Linuxbrew include directory too.
+      include_directories(SYSTEM "${LIBCXX_INCLUDE_DIR}")
+
+      execute_process(COMMAND "${CMAKE_CXX_COMPILER}" -print-search-dirs
+                      OUTPUT_VARIABLE CLANG_PRINT_SEARCH_DIRS_OUTPUT)
+
+      if ("${CLANG_PRINT_SEARCH_DIRS_OUTPUT}" MATCHES ".*libraries: =([^:\r\n]+)(:.*|[\r\n]*$)" )
+        # We get a directory like this:
+        # .../yb-llvm-v12.0.1-yb-1-1639783720-bdb147e6-almalinux8-x86_64/lib/clang/12.0.1
+        set(CLANG_LIB_DIR "${CMAKE_MATCH_1}")
+        if(APPLE)
+          set(CLANG_RUNTIME_LIB_DIR "${CLANG_LIB_DIR}/lib/darwin")
           if(NOT EXISTS "${CLANG_RUNTIME_LIB_DIR}")
             message(FATAL_ERROR
                     "Failed to determine Clang runtime library directory inside of "
                     "${CLANG_RUNTIME_LIB_DIR}")
           endif()
+        else()
+          set(CLANG_RUNTIME_LIB_DIR "${CLANG_LIB_DIR}/lib/linux")
+          if(NOT EXISTS "${CLANG_RUNTIME_LIB_DIR}")
+            set(CLANG_RUNTIME_LIB_DIR
+                "${CLANG_LIB_DIR}/lib/${CMAKE_SYSTEM_PROCESSOR}-unknown-linux-gnu")
+            if(NOT EXISTS "${CLANG_RUNTIME_LIB_DIR}")
+              message(FATAL_ERROR
+                      "Failed to determine Clang runtime library directory inside of "
+                      "${CLANG_RUNTIME_LIB_DIR}")
+            endif()
+          endif()
         endif()
+      else()
+        message(FATAL_ERROR
+                "Could not parse the output of 'clang -print-search-dirs': "
+                "${CLANG_PRINT_SEARCH_DIRS_OUTPUT}")
       endif()
-    else()
-      message(FATAL_ERROR
-              "Could not parse the output of 'clang -print-search-dirs': "
-              "${CLANG_PRINT_SEARCH_DIRS_OUTPUT}")
-    endif()
-    if(USING_LINUXBREW)
-      set(CLANG_INCLUDE_DIR "${CLANG_LIB_DIR}/include")
-      if(NOT EXISTS "${CLANG_INCLUDE_DIR}")
-        message(FATAL_ERROR "Clang include directory '${CLANG_INCLUDE_DIR}' does not exist")
+      if(USING_LINUXBREW)
+        set(CLANG_INCLUDE_DIR "${CLANG_LIB_DIR}/include")
+        if(NOT EXISTS "${CLANG_INCLUDE_DIR}")
+          message(FATAL_ERROR "Clang include directory '${CLANG_INCLUDE_DIR}' does not exist")
+        endif()
+        ADD_CXX_FLAGS("-isystem ${CLANG_INCLUDE_DIR}")
+      endif()
+
+      ADD_CXX_FLAGS("-nostdinc++")
+      if(USING_LINUXBREW)
+        ADD_CXX_FLAGS("-nostdinc")
       endif()
-      ADD_CXX_FLAGS("-isystem ${CLANG_INCLUDE_DIR}")
     endif()
 
     if ("${COMPILER_VERSION}" VERSION_GREATER_EQUAL "12.0.0")
       ADD_LINKER_FLAGS("-fuse-ld=lld")
       ADD_LINKER_FLAGS("-lunwind")
     endif()
-
-    ADD_CXX_FLAGS("-nostdinc++")
-    if(USING_LINUXBREW)
-      ADD_CXX_FLAGS("-nostdinc")
-    endif()
   endif()
 endmacro()
 
diff -urN orig/yugabyte-db-2026.1.1.0/python/yugabyte/build_postgres.py new/yugabyte-db-2026.1.1.0/python/yugabyte/build_postgres.py
--- a/python/yugabyte/build_postgres.py	2026-08-06 03:52:33.000000000 +0200
+++ b/python/yugabyte/build_postgres.py	2026-08-17 13:47:51.992905625 +0200
@@ -17,6 +17,7 @@
 directories.
 """
 
+import hashlib
 import logging
 import multiprocessing
 import os
@@ -702,12 +703,32 @@
                     'src/postgres/.clang-format',
                     'src/yb/yql/pggate/**/README',
                 ]))
-            # Get the most recent commit that touched postgres files.
-            git_hash = subprocess.check_output(
-                ['git', '--no-pager', 'log', '-n', '1', '--format=%H', '--'] + pathspec
-            ).decode('utf-8').strip()
-            # Get uncommitted changes to tracked postgres files.
-            git_diff = subprocess.check_output(['git', 'diff', 'HEAD', '--'] + pathspec)
+            # Distro tarballs have no .git. Fall back to hashing the listed paths.
+            try:
+                git_hash = subprocess.check_output(
+                    ['git', '--no-pager', 'log', '-n', '1', '--format=%H', '--'] + pathspec,
+                    stderr=subprocess.DEVNULL,
+                ).decode('utf-8').strip()
+                git_diff = subprocess.check_output(['git', 'diff', 'HEAD', '--'] + pathspec)
+            except (subprocess.CalledProcessError, FileNotFoundError):
+                hasher = hashlib.sha256()
+                for root_item in ('CMakeLists.txt', 'src/postgres', 'src/yb/yql/pggate'):
+                    path = os.path.join(YB_SRC_ROOT, root_item)
+                    if os.path.isfile(path):
+                        with open(path, 'rb') as inp:
+                            hasher.update(inp.read())
+                    elif os.path.isdir(path):
+                        for dirpath, _, filenames in os.walk(path):
+                            for name in sorted(filenames):
+                                fpath = os.path.join(dirpath, name)
+                                hasher.update(name.encode())
+                                try:
+                                    with open(fpath, 'rb') as inp:
+                                        hasher.update(inp.read(65536))
+                                except OSError:
+                                    pass
+                git_hash = 'file_sha256=' + hasher.hexdigest()
+                git_diff = b''
 
         env_vars_str = self.get_env_vars_str(self.env_vars_for_build_stamp)
         build_stamp = "\n".join([
diff -urN orig/yugabyte-db-2026.1.1.0/src/postgres/CMakeLists.txt new/yugabyte-db-2026.1.1.0/src/postgres/CMakeLists.txt
--- a/src/postgres/CMakeLists.txt	2026-08-06 03:52:33.000000000 +0200
+++ b/src/postgres/CMakeLists.txt	2026-08-17 13:47:51.992232677 +0200
@@ -71,6 +71,11 @@
      (IS_APPLE_CLANG AND "${COMPILER_VERSION}" VERSION_GREATER_EQUAL "14.0.3"))
     add_pg_c_cxx_flags(-Wno-error=deprecated-non-prototype)
   endif()
+
+  # C23 / newer libc make strchr(const char*) return const char*.
+  if("${COMPILER_VERSION}" VERSION_GREATER_EQUAL "16.0.0")
+    add_pg_c_cxx_flags(-Wno-error=incompatible-pointer-types-discards-qualifiers)
+  endif()
 endif()
 
 if(APPLE)
@@ -182,9 +187,13 @@
   endif()
 
   if(IS_CLANG AND NOT IS_ASAN)
-    # Skip adding -lc++ when ASAN is enabled, as it's already added globally via ADD_LINKER_FLAGS
-    # in YugabyteFunctions.cmake to avoid duplicate library warnings.
-    string(APPEND PG_EXE_LD_FLAGS_AFTER_YB_LIBS " -lc++")
+    if(DEFINED ENV{YB_USE_SYSTEM_COMPILER} AND "$ENV{YB_USE_SYSTEM_COMPILER}" STREQUAL "1")
+      string(APPEND PG_EXE_LD_FLAGS_AFTER_YB_LIBS " -lstdc++ -latomic")
+    else()
+      # Skip adding -lc++ when ASAN is enabled, as it's already added globally via ADD_LINKER_FLAGS
+      # in YugabyteFunctions.cmake to avoid duplicate library warnings.
+      string(APPEND PG_EXE_LD_FLAGS_AFTER_YB_LIBS " -lc++")
+    endif()
   endif()
   if(IS_GCC)
     # To avoid these errors when linking postgres executables:
diff -urN orig/yugabyte-db-2026.1.1.0/src/yb/util/CMakeLists.txt new/yugabyte-db-2026.1.1.0/src/yb/util/CMakeLists.txt
--- a/src/yb/util/CMakeLists.txt	2026-08-06 03:52:33.000000000 +0200
+++ b/src/yb/util/CMakeLists.txt	2026-08-17 13:47:51.993235419 +0200
@@ -258,7 +258,8 @@
   opentelemetry_trace
   opentelemetry_exporter_otlp_http
   opentelemetry_resources
-  opentelemetry_proto)
+  opentelemetry_proto
+  opentelemetry_common)
 
 set(UTIL_LIBS
   absl
diff -urN orig/yugabyte-db-2026.1.1.0/yb_build.sh new/yugabyte-db-2026.1.1.0/yb_build.sh
--- a/yb_build.sh	2026-08-06 03:52:33.000000000 +0200
+++ b/yb_build.sh	2026-08-17 13:47:51.993130277 +0200
@@ -1017,6 +1017,9 @@
     fatal "Google TCMalloc is only supported on linux. OSTYPE is: '${OSTYPE}'."
   fi
   cmake_opts+=( "-DYB_GOOGLE_TCMALLOC=1" )
+else
+  # Always pass 0 so a cached 1 from a previous configure cannot stick.
+  cmake_opts+=( "-DYB_GOOGLE_TCMALLOC=0" )
 fi
 
 if [[ $build_type == "prof_gen" ]]; then
